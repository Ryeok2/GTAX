{
    "private": true,
    "dependencies": {
        "@openzeppelin/contracts": "^3.3.0",
        "truffle-hdwallet-provider": "^1.0.17",
        "web3": "^1.3.3"
    },
    "scripts": {
        "compile": "truffle compile",
        "deploylocal": "truffle migrate --network development",
        "deployrinkeby": "truffle migrate --network rinkeby",
        "deploylive": "truffle migrate --network live",
        "build-contracts": "sol-merger \"./contracts/*.sol\" ./build"
    },
    "devDependencies": {
        "sol-merger": "^3.1.0"
    }
}
